<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Slacker News</title>
    <link rel="stylesheet" href="/styles.css">
  </head>

  <body>
    <header>
    <ul class="navigation">
      <li><img id = "logo-image" src = "/y18.gif"></li>
      <li><a id = "brand" href = "/">Hacker News</a></li>
      <li><a class = "nav-link" href = "/new">new</a></li>
      <li><a class = "nav-link" href = "#">comments</a></li>
      <li><a class = "nav-link" href = "#">ask</a></li>
      <li><a class = "nav-link" href = "#">jobs</a></li>
      <li><a class = "nav-link-last" href = "#">submit</a></li>
      <a id = 'login' href = "#">login</a>
    </ul>
    </header>

    <!--This is our link list -->
    <div class = "main-container">
      <ol class = "links">
        <% @articles.each do |article| %>
          <li>
            <p><img class = "arrow" src = "/grayarrow.gif">
            <a href=<%="#{article['url']}" %>><%="#{article['headline']}"%></a></p>
            <p><%="#{article['description']}" %>
            <p>posted by: <%="#{article['name']}" %> | <a href="/articles/<%= "#{article['id']}" %>/comments">comments</a></p>
            <p></p>
              <ul class= "comments">
              <% @articles.each do |comment| %>
                <li>
                  <p><%= comment['content'] %></p>
                  <p>posted by: <%= comment['commenter']%></p>
                </li>

                <%end%>
                </ul>
          </li>
          <%break%>
        <% end %>
      </ol>
      <form action="/comments/<%= params[:id]%>" method="post">
          <label for="new_comment">Name:</label><br />
          <input type="text" id="new_comment" name="name" value="<%= params[:name] %>" size="40" required/><br />
          <label for="comments">Comment:</label><br />
          <textarea id="comments" name="comments" rows="10" cols="50" required></textarea><br />
          <input type="submit" value="Submit" />
          </form>
    </div>
  </body>
</html>
